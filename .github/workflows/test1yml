name: File operations in github

on: workflow_dispatch

jobs:
    files:
        runs-on: ubuntu-latest
        steps:
            - name: create a one file
              run: echo "Welcome to Github Actions" > file1.txt

            - name: display the content of a file
              run: cat file1.txt

            - name: display the date
              run: date
